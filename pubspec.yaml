name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  shared_preferences: ^2.5.3  
  http: ^0.13.6
  provider: ^6.0.5
  geolocator: ^14.0.0
  flutter_spinkit: ^5.1.0
  flutter_dotenv: ^5.1.0
  flutter_typeahead: ^4.3.7
  
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  assets: 
    - lib/flutterapikey.env
    
  uses-material-design: true